
use std::path::Path;
use ini::Ini;

fn read_parameter_from_ini(ini_file_name: String, parameter_in: String) -> f64 {
  // let ini_path = Path::new(".").join("parameters").join(ini_file_name);
  // let ini = Ini::load_from_file(ini_path).unwrap();
  // for (sec, prop) in ini.iter() {

  //     for (k, v) in prop.iter() {
  //         println!("{}:{}", k, v);
  //     }
  // }
  let test: f64 = 0.0;
  test
}